import { Component } from "hyperapp"
import { WraperProperties } from '../_utils/interfaces';

export interface LoadingProperties extends WraperProperties {}

/**
 * Loading component
 */
declare const Loading: Component<LoadingProperties>

export default Loading
